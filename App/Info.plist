<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!-- The commit this build came from, which DragonKit renders after the build number:
	     "v2.13.0 (1346) · 2026-Aug-09 12:00:00 UTC". The release workflow passes
	     `git log -1 --format=%cI` as the DRAGON_COMMIT_DATE build setting; mapping it here is
	     what makes it reach the app, because xcodebuild hands back a SIGNED bundle and editing
	     Info.plist afterwards would break the signature. Expands to empty in a plain local
	     build, and DragonKit then shows no timestamp at all rather than a misleading one. -->
	<key>DragonCommitDate</key>
	<string>$(DRAGON_COMMIT_DATE)</string>
	<key>LSUIElement</key>
	<true/>
	<key>SUFeedURL</key>
	<string>https://raw.githubusercontent.com/teddychan/ice-2/main/docs/ice-2/appcast.xml</string>
	<key>SUPublicEDKey</key>
	<string>p+F/ivF5bAYcmuNuCMNHcRv123A6LHFpCBagFm7Adu8=</string>
	<!-- The default for scheduled background update checks. Two jobs: it stops Sparkle
	     from ever showing its one-time "check for updates automatically?" permission
	     prompt (what the removed SPUUpdaterDelegate did by returning NO from
	     updaterShouldPromptForPermissionToCheckForUpdates:), and it states the default
	     Sparkle already applied while the key was absent. A user's own choice in
	     Settings ▸ Updates is stored in user defaults, which takes precedence. -->
	<key>SUEnableAutomaticChecks</key>
	<false/>
</dict>
</plist>
